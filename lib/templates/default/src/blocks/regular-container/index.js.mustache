/**
 * WordPress dependencies
 */
import { __ } from '@wordpress/i18n';

/**
 * Internal dependencies
 */
import metadata from './block.json';
import Edit from './edit';
import Save from './save';

/**
 * Style dependencies
 */
import './editor.scss';
import './style.scss';

/**
 * Block constants
 */
const { name, category, attributes, supports } = metadata;

const settings = {
	title: __( 'Container', '{{textdomain}}' ),
	description: __( 'Provide custom container.', '{{textdomain}}' ),
	icon: 'editor-kitchensink',
	keywords: [
		__( 'container', '{{textdomain}}' ),
		__( 'wrapper', '{{textdomain}}' ),
		__( 'section', '{{textdomain}}' ),
	],
	attributes,
	supports,
	edit: Edit,
	save: Save,
};
export { name, category, settings };
